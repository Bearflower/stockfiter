# 验收清单

## 阶段一：操作记录 opinion 补全

- [x] Task 1: 操作记录 opinion 自动生成
  - [x] 1.1 从操作时间线加载的记录，opinion 字段非空
  - [x] 1.2 opinion 格式为 `{plan}计划 {action}{fund}({code})`，A股基金格式正确
  - [x] 1.3 无 code 的记录不显示空括号

## 阶段二：检索分层与主题过滤

- [x] Task 2: 分层检索
  - [x] 2.1 指定 source_type=operation 时仅返回操作记录
  - [x] 2.2 指定 source_type=observation 时仅返回市场判断
  - [x] 2.3 指定 source_type=principle 时仅返回通用原则
  - [x] 2.4 `_search_all_layered` 返回三种类型，涉及品种的查询各类型至少一条

- [x] Task 3: 主题过滤
  - [x] 3.1 查询 "中证500" 时，匹配到 "交易操作" 主题
  - [x] 3.2 查询 "红利" 时，observation 返回红利相关判断
  - [x] 3.3 查询 "高估值" 时，匹配到 "风险控制"、"估值判断" 主题

## 阶段三：向量模型升级

- [x] Task 4: 模型下载超时
  - [x] 4.1 无网络时 30 秒内降级到 TF-IDF，不无限等待
  - [x] 4.2 降级日志包含原因说明
  - [x] 4.3 有网络时成功下载 sentence-transformers 模型（如果网络可达）
  - [x] 4.4 已下载的模型缓存后第二次加载 < 5 秒

## 阶段四：排序优化

- [x] Task 5: 品种匹配加分
  - [x] 5.1 查询 "中证500" 时，中证500 相关记录排在非中证500 前面
  - [x] 5.2 查询 "红利" 时，红利相关记录排在前面
  - [x] 5.3 混合排序权重：BM25 0.2、向量 0.5、品种匹配 0.3

## 阶段五：精度回测对比

- [x] Task 6: 精度对比
  - [x] 6.1 三个不同查询返回不同的 Top-5 结果（不再全是同一批记录）
  - [x] 6.2 查询 "中证500" 返回结果中包含中证500 操作记录 (opinion 非空)
  - [x] 6.3 查询 "红利" 返回结果中包含红利相关操作和判断
  - [x] 6.4 查询 "高估值" 返回结果中包含估值判断主题的记录

## 整体质量

- [x] 不修改任何 JSONL 数据文件
- [x] 所有新增参数从 config.yaml 读取
- [x] 向量模型不可用时安全降级，不影响系统运行
- [x] 检索结果按 source_type 分层返回，标注清晰