# Codex 项目协作约定

## 适用范围

本文件适用于整个仓库。开始工作时先确认仓库根目录、当前分支、工作区状态，以及与任务最相关的 `README.md`、`docs/` 文档和现有实现。

## 始终遵守

- 默认使用中文沟通；代码标识符遵循现有惯例，注释与日志优先使用中文。
- 项目入口见 `README.md`，完整文档索引见 `docs/README.md`。需求、设计、计划、审计和部署文档统一放入 `docs/` 对应目录。
- 策略阈值、评分、调度时间和风险参数必须来自配置或可追溯的计算，不得添加无依据的硬编码值。
- 禁止在生产服务器运行回测。服务器只可用于数据筛选或导出；所有回测和参数优化必须在本地完成。
- 修改前读取相关代码、配置和文档；修改后运行范围匹配的测试、静态检查或可观察验证，并根据实际输出汇报。
- 保留用户已有改动；不要改写无关文件，不使用破坏性 Git 命令清理工作区。
- 未经明确要求，不提交、推送、部署、修改远程系统或执行难以恢复的操作。授权一个动作不自动授权后续动作。

## 工作流路由

- 修改代码、配置、测试、策略或运行行为时，使用 `project-development-workflow`。
- 用户明确要求暂存、提交、推送或输入 `/git` 时，使用 `project-git-workflow`。
- 用户明确要求部署、发布或输入 `/deploy` 时，使用 `project-deployment-workflow`。
- 用户明确要求完成、归档或输入 `/finish` 时，使用 `project-task-archive`。

详细历史规则保存在 `.trae/rules/`。这些规则需要按当前仓库事实和 Codex 能力适配：Trae 专属工具、斜杠命令和智能体名称表达的是职责，不代表当前环境存在同名能力。不得伪造调用；用户指令、平台安全边界和真实项目配置优先于旧示例。

## 文档与验证

- 行为、API、配置、部署方式或操作流程变化时，检查并更新相关文档；纯内部实现且不影响使用方式时可说明无需更新。
- 完成声明必须附带新鲜验证证据。无法运行的验证要说明原因、未验证部分和风险。
- Git、部署和任务归档是独立授权域；完成开发不代表自动提交、推送、部署或归档任务。
