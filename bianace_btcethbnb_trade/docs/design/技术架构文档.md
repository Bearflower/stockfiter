# 币安自动化交易系统 - 技术架构文档

## 1. 系统概述

### 1.1 系统简介

币安自动化交易系统是一个基于规则引擎的量化交易系统，支持 PM 账户（投资组合保证金账户）。系统基于 `traderule.txt` 规则，实现从信号检测、仓位计算、风险管理到订单生成的全流程自动化。

**系统版本**: v6.14.0 (2026-04-27)

**核心特性**:
- ✅ 多时间框架技术分析（日线 +4 小时 +1 小时）
- ✅ 量化评分系统（v6.12 多维度评分）
- ✅ 动态仓位管理（v6.13 智能降仓）
- ✅ ATR 动态止损止盈（v6.13.1 优化）
- ✅ 频率控制与风险管理
- ✅ 飞书通知与交易日报
- ✅ Docker 容器化部署
- ✅ 模块化架构（v1.0.0 重构）

### 1.2 设计原则

1. **规则驱动**: 严格遵循 traderule.txt 规则引擎
2. **风险优先**: 多层风控机制，确保资金安全
3. **模块化**: 核心模块解耦，便于维护和扩展
4. **可回测**: 完整的历史数据回测系统
5. **可观测**: 详细日志 + 飞书通知 + 数据库记录

---

## 2. 系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                       定时调度层                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  scheduler_new.py (APScheduler)                     │    │
│  │  - 每小时整点触发 (00:00, 01:00, ..., 23:00)        │    │
│  │  - 每日 9:00 发送交易日报                            │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                       核心服务层                               │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐   │
│  │ 数据获取  │ │ 信号检测  │ │ 仓位计算  │ │ 风险管理  │   │
│  │ 模块      │ │ 模块      │ │ 模块      │ │ 模块      │   │
│  └───────────┘ └───────────┘ └───────────┘ └───────────┘   │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐   │
│  │ 订单生成  │ │ 应急处理  │ │ 频率控制  │ │ 仓位调整  │   │
│  │ 模块      │ │ 模块      │ │ (v6.12)   │ │ (v6.13)   │   │
│  └───────────┘ └───────────┘ └───────────┘ └───────────┘   │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                       支撑服务层                               │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐   │
│  │ 币安 API  │ │ 飞书通知  │ │ 数据库    │ │ 技术指标  │   │
│  │ 封装      │ │ 服务      │ │ 管理器    │ │ 计算      │   │
│  │           │ │ (通用服务)│ │           │ │           │   │
│  └───────────┘ └───────────┘ └───────────┘ └───────────┘   │
│  ┌───────────┐ ┌───────────┐                                │
│  │ K 线服务  │ │ 通知服务  │                                │
│  │ (通用服务)│ │ (通用服务)│                                │
│  └───────────┘ └───────────┘                                │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                       基础设施层                               │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐   │
│  │ Docker    │ │ 日志系统  │ │ 配置管理  │ │ 环境变量  │   │
│  │ 容器      │ │ (Rotating)│ │ (YAML)    │ │ (.env)    │   │
│  └───────────┘ └───────────┘ └───────────┘ └───────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 技术栈

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| **调度框架** | APScheduler 3.10.4 | 定时任务调度 |
| **Web 框架** | FastAPI 0.104.1 | API 接口（可选） |
| **数据库** | PostgreSQL + SQLAlchemy 2.0.23 | 数据持久化 |
| **数据处理** | Pandas 2.2.1 + NumPy 1.26.4 | K 线数据处理与指标计算 |
| **HTTP 客户端** | Requests 2.31.0 | 币安 API 调用 |
| **配置管理** | PyYAML 6.0.1 + python-dotenv | YAML 配置 + 环境变量 |
| **容器化** | Docker + docker-compose | 容器化部署 |
| **日志** | Python logging (RotatingFileHandler) | 滚动日志 |

---

## 3. 核心模块设计

### 3.1 模块依赖关系

**重构后架构（v1.0.0 - 2026-04-27）**：

```
scheduler/ (调度器模块 - 拆分为6个子模块)
    │
    ├─→ scheduler/scheduler.py       # 调度器核心（APScheduler配置）
    ├─→ scheduler/analyzer.py        # 分析流程（信号检测、评分）
    ├─→ scheduler/trade_executor.py  # 交易执行（开仓、平仓）
    ├─→ scheduler/statistics.py      # 统计功能（胜率、日报）
    ├─→ scheduler/notifier.py        # 通知功能（飞书推送）
    │
    ├─→ core/data/                   # 数据模块（拆分为3个子模块）
    │       ├─→ fetcher.py           # 数据获取（支持并发）
    │       ├─→ indicators.py        # 指标计算
    │       └─→ cache.py             # 缓存管理（TTL + LRU）
    │
    ├─→ core/signal/                 # 信号模块（拆分为3个子模块）
    │       ├─→ detector.py          # 信号检测核心
    │       ├─→ filter.py            # 过滤器（ADX、成交量、ATR）
    │       └─→ validator.py         # 验证器（一票否决项）
    │
    ├─→ core/scoring/                # 评分模块（重构为工厂模式）
    │       ├─→ base.py              # 评分引擎基类
    │       ├─→ v612.py              # v6.12版本实现
    │       └─→ factory.py           # 工厂模式
    │
    ├─→ core/position_calculator.py  # 仓位计算
    │       └─→ config/config_manager.py
    │
    ├─→ core/risk_manager.py         # 风险管理
    │       ├─→ utils/atr_calculator.py
    │       └─→ config/config_manager.py
    │
    ├─→ core/order_generator.py      # 订单生成
    │       └─→ utils/binance_trade_api.py
    │
    ├─→ core/emergency_handler.py    # 应急处理
    │       └─→ config/config_manager.py
    │
    ├─→ services/                    # 服务层（基于BaseService重构）
    │       ├─→ base.py              # 服务基类（新增）
    │       ├─→ frequency_controller.py  # 频率控制 (v6.12)
    │       ├─→ rule_executor.py     # 规则执行器（重构）
    │       └─→ trade_executor.py    # 交易执行器（重构）
    │
    └─→ models/                      # 数据模型层（新增数据仓库模式）
            ├─→ repository.py        # 数据仓库基类（新增）
            ├─→ entities.py          # 具体数据仓库实现（新增）
            └─→ database.py          # 数据库连接管理
```

**重构改进点**：
1. **模块拆分** - 将大型模块拆分为职责单一的子模块
2. **服务基类** - 创建统一的BaseService，规范服务层接口
3. **数据仓库** - 引入Repository Pattern，分离数据访问逻辑
4. **工厂模式** - 评分引擎使用工厂模式，支持版本管理
5. **缓存增强** - 数据缓存支持TTL和LRU策略

### 3.2 核心模块职责

#### 3.2.1 调度器模块 (scheduler/)

**重构说明** (v1.0.0 - 2026-04-27):
- 将原 `scheduler_new.py` (904行) 拆分为6个职责单一的子模块
- 提高代码可维护性和可测试性
- 每个子模块独立测试和部署

**模块结构**:

1. **scheduler/scheduler.py** - 调度器核心
   - APScheduler 配置和任务调度
   - 定时任务管理（每小时分析、每日日报）
   - 协调各子模块执行流程

2. **scheduler/analyzer.py** - 分析流程
   - 行情分析入口
   - 信号检测调用
   - 评分系统集成

3. **scheduler/trade_executor.py** - 交易执行
   - 开仓执行
   - 平仓执行
   - 订单管理

4. **scheduler/statistics.py** - 统计功能
   - 胜率统计
   - 交易日报生成
   - 绩效分析

5. **scheduler/notifier.py** - 通知功能
   - 飞书推送
   - 消息模板管理
   - 通知策略

**关键类**:
```python
# scheduler/scheduler.py
class RuleEngineScheduler:
    - __init__(enable_auto_trade: bool)
    - start_scheduler()
    - stop_scheduler()

# scheduler/analyzer.py
class MarketAnalyzer:
    - analyze_market(symbol: str) -> Dict
    - detect_signals(data: Dict) -> List[Signal]

# scheduler/trade_executor.py
class TradeExecutor:
    - execute_trade(signal: Dict) -> bool
    - close_position(position_id: int) -> bool

# scheduler/statistics.py
class StatisticsManager:
    - update_statistics(trade: Dict)
    - generate_daily_report() -> Dict

# scheduler/notifier.py
class NotificationManager:
    - send_notification(message: str, level: str)
    - send_trade_notification(trade: Dict)
```

**执行流程**:
```
1. 获取多时间框架 K 线数据 (1d, 4h, 1h)
   ↓
2. 检测交易信号 (评分系统)
   ↓
3. 检查频率控制限制 (v6.12)
   ↓
4. 计算仓位 (v6.13 动态调整)
   ↓
5. 计算止损止盈价格
   ↓
6. 生成订单参数
   ↓
7. 检查应急处理条件
   ↓
8. 执行交易 (可选) + 发送飞书通知
   ↓
9. 记录数据库
```

#### 3.2.2 数据模块 (core/data/)

**重构说明** (v1.0.0 - 2026-04-27):
- 将原 `core/data_fetcher.py` (563行) 拆分为3个子模块
- 增强缓存功能，支持TTL和LRU策略
- 添加并发数据获取支持，提升性能

**模块结构**:

1. **core/data/fetcher.py** - 数据获取（增强版）
   - 从通用K线服务获取多时间框架数据
   - 支持并发获取多个交易对数据（`_fetch_from_kline_service_concurrent`）
   - 错误重试和降级策略
   - 性能统计（`get_performance_stats`）
   - 并发超时控制（30秒）

2. **core/data/indicators.py** - 指标计算
   - 技术指标计算（EMA、MACD、RSI、ATR等）
   - 多时间框架指标聚合
   - 指标缓存管理

3. **core/data/cache.py** - 缓存管理
   - TTL（Time To Live）缓存过期策略
   - LRU（Least Recently Used）缓存淘汰策略
   - 缓存命中率统计

**关键类**:
```python
# core/data/fetcher.py
class MarketDataFetcher:
    - __init__(config_manager: ConfigManager)
    - fetch_multi_timeframe_data(symbol: str, timeframes: List[str]) -> Dict
    - fetch_concurrent_data(symbols: List[str]) -> Dict[str, Dict]

# core/data/indicators.py
class IndicatorCalculator:
    - calculate_ema(data: List, period: int) -> List
    - calculate_macd(data: List) -> Dict
    - calculate_rsi(data: List, period: int) -> List
    - calculate_atr(data: List, period: int) -> List

# core/data/cache.py
class DataCache:
    - __init__(ttl: int = 300, max_size: int = 1000)
    - get(key: str) -> Optional[Any]
    - set(key: str, value: Any)
    - get_stats() -> Dict
```

**数据格式**:
```python
{
    '1d': [
        {
            'timestamp': '2026-04-12T00:00:00',
            'open': '95000.00',
            'high': '96000.00',
            'low': '94000.00',
            'close': '95500.00',
            'volume': '1234.56'
        },
        ...
    ],
    '4h': [...],
    '1h': [...]
}
```

#### 3.2.3 信号检测模块 (core/signal/)

**重构说明** (v1.0.0 - 2026-04-27):
- 将原 `core/signal_detector.py` (502行) 拆分为3个子模块
- 职责分离：检测、过滤、验证独立管理
- 提高代码可测试性和可维护性

**模块结构**:

1. **core/signal/detector.py** - 信号检测核心
   - 信号检测主流程
   - 评分系统集成（v5.5 新增）
   - 信号分级输出
   - Decimal 类型安全转换（价格计算、仓位参数）

2. **core/signal/filter.py** - 过滤器
   - ADX 趋势强度过滤（`min_adx` 参数，默认 `Decimal('20')`）
   - 成交量放大检测
   - ATR% 区间过滤（2.0% ~ 4.5%）
   - 自动处理 float/Decimal 类型转换

3. **core/signal/validator.py** - 验证器
   - 资金费率验证
   - 波动率验证
   - 24h 涨跌幅验证
   - 数据完整性验证

**核心流程**:
```
1. 前置过滤器 (core/signal/filter.py)
   ├─ ADX ≥ 20 (日线级别)
   ├─ 成交量放大 (S 级≥1.8 倍，A 级≥1.5 倍，B/C 级≥1.2 倍)
   └─ ATR% 区间：2.0% ~ 4.5%
   
   ↓
   
2. 量化评分 (core/scoring/)
   ├─ 趋势强度 (40 分): EMA21 斜率 + 价格距离
   ├─ 形态质量 (35 分): K 线形态 + 突破识别
   └─ 动量背离 (25 分): MACD 柱线背离
   
   ↓
   
3. 一票否决 (core/signal/validator.py)
   ├─ 资金费率 > 0.08%
   ├─ 波动率 > 阈值 (币种差异化)
   ├─ 24h 涨跌幅超限
   └─ 数据完整性不足
   
   ↓
   
4. 信号分级
   ├─ S 级 (≥85 分): 仓位 50%, 杠杆 5x
   ├─ A 级 (≥75 分): 仓位 30%, 杠杆 4x
   ├─ B 级 (≥65 分): 仓位 15%, 杠杆 3x
   └─ C 级 (≥55 分): 仓位 5%, 杠杆 2x
```

**关键类**:
```python
# core/signal/detector.py
class SignalDetector:
    - __init__(config_manager: ConfigManager, scoring_engine: ScoringEngineBase)
    - detect_signals(symbol: str, klines: Dict) -> List[Signal]
    - _calculate_score(data: Dict) -> ScoreResult

# core/signal/filter.py
class SignalFilter:
    - __init__(config_manager: ConfigManager)
    - check_filters(data: Dict) -> bool
    - check_adx(data: Dict) -> bool
    - check_volume(data: Dict) -> bool
    - check_atr(data: Dict) -> bool

# core/signal/validator.py
class SignalValidator:
    - __init__(config_manager: ConfigManager)
    - validate(data: Dict) -> Tuple[bool, Optional[str]]
    - check_funding_rate(data: Dict) -> bool
    - check_volatility(data: Dict) -> bool
    - check_price_change(data: Dict) -> bool
```

#### 3.2.4 评分引擎模块 (core/scoring/)

**重构说明** (v1.0.0 - 2026-04-27):
- 将评分引擎重构为工厂模式，支持版本管理
- 创建统一的评分引擎基类，规范接口
- 便于未来添加新版本评分引擎

**模块结构**:

1. **core/scoring/base.py** - 评分引擎基类
   - 定义统一的评分接口
   - 提供通用的评分方法
   - 抽象方法约束子类实现

2. **core/scoring/v612.py** - v6.12版本实现
   - 多维度量化评分系统
   - 趋势强度、形态质量、动量背离评分
   - 仓位系数映射

3. **core/scoring/factory.py** - 工厂模式
   - 评分引擎工厂类
   - 版本管理和选择
   - 单例模式支持

**评分维度** (v6.12):
```python
评分维度 (总分 100 分):
├─ 趋势强度 (40 分)
│   ├─ EMA21 斜率归一化 (0-20 分)
│   └─ 价格与 EMA21 距离 (0-20 分)
│
├─ 形态质量 (35 分)
│   ├─ K 线形态识别 (0-21 分)
│   │   ├─ 阳包阴/阴包阳
│   │   ├─ 突破回踩
│   │   └─ 背离形态
│   └─ 支撑阻力突破 (0-9 分)
│
└─ 动量背离 (25 分)
    ├─ RSI 评分 (0-8 分)
    └─ MACD 柱线背离 (0-12 分)
```

**关键类**:
```python
# core/scoring/base.py
class ScoringEngineBase(ABC):
    - __init__(config_manager: ConfigManager)
    - score(symbol: str, data: Dict) -> ScoreResult  # 抽象方法
    - _calculate_trend_strength(data: Dict) -> float
    - _calculate_pattern_quality(data: Dict) -> float
    - _calculate_momentum_divergence(data: Dict) -> float

# core/scoring/v612.py
class ScoringEngineV612(ScoringEngineBase):
    - __init__(config_manager: ConfigManager)
    - score(symbol: str, data: Dict) -> ScoreResult
    - _apply_v612_weights(scores: Dict) -> float

# core/scoring/factory.py
class ScoringEngineFactory:
    - __init__()
    - get_engine(version: str = 'v6.12') -> ScoringEngineBase
    - list_available_versions() -> List[str]

# 工厂函数
def get_scoring_engine(version: str = 'v6.12') -> ScoringEngineBase
def get_scoring_engine_v612() -> ScoringEngineV612
def create_scoring_engine(version: str, config_manager: ConfigManager) -> ScoringEngineBase
def list_available_versions() -> List[str]
```

**仓位系数映射**:
```python
def score_to_position_ratio(score: float, grade: str) -> float:
    """
    S 级 (≥85 分): 50% 仓位系数
    A 级 (≥75 分): 30% 仓位系数
    B 级 (≥65 分): 15% 仓位系数
    C 级 (≥55 分): 5% 仓位系数
    """
```

**关键技术**:
- 使用 `numpy.polyfit()` 计算 EMA 斜率
- 多时间框架数据一致性检查
- 币种差异化配置 (BTC/ETH/BNB 不同阈值)
- 工厂模式支持版本切换

#### 3.2.5 仓位计算模块 (core/position_calculator.py)

**职责**: 根据信号等级和止损距离计算仓位大小

**核心公式**:
```python
# 1. 计算基础名义价值
risk_distance = abs(entry_price - stop_loss_price)
base_notional_value = (single_position_margin * leverage) / risk_distance

# 2. 应用仓位系数 (v5.3)
position_coefficient = get_position_coefficient(grade)  # S:0.5, A:0.3, B:0.15, C:0.05
actual_notional_value = base_notional_value * position_coefficient

# 3. 计算实际仓位
quantity = actual_notional_value / entry_price
margin = actual_notional_value / leverage
```

**v6.13 动态仓位调整**:
```python
# services/position_adjuster.py
def adjust_position(
    symbol: str,
    position_params: Dict,
    available_balance: Decimal
) -> Optional[Dict]:
    """
    1. 计算可用保证金 = 可用余额 × 0.8 (保留 20% 安全垫)
    2. 如果 可用保证金 >= 预设保证金：不调整
    3. 如果 可用保证金 < 预设保证金:
       - 计算调整系数 = 可用保证金 / 预设保证金
       - 调整后保证金 = 预设保证金 × 调整系数
       - 如果 调整后保证金 < 5U：跳过交易
       - 否则：等比缩放仓位
    """
```

#### 3.2.6 风险管理模块 (core/risk_manager.py)

**职责**: ATR 动态止损止盈计算

**止损策略** (v6.13.1):
```python
def calculate_atr_based_stop_loss(
    entry_price: Decimal,
    direction: int,
    atr14: Decimal,
    max_stop_pct: Decimal = Decimal('0.07')
) -> Tuple[Decimal, Decimal]:
    """
    止损距离 = 1.5 × ATR14
    最大止损 = 7% (防止极端情况)
    """
```

**止盈策略** (v6.13.1 优化):
```python
def calculate_take_profit_levels(
    entry_price: Decimal,
    direction: int,
    atr14: Decimal,
    signal_grade: str
) -> List[TakeProfitLevel]:
    """
    TP1 = 2.5 × ATR, 平仓 25%
    TP2 = 4.0 × ATR, 平仓 25%
    剩余 50% 使用吊灯止损:
      - 启动阈值：1.8 × ATR
      - 回撤阈值：1.2 × ATR
    """
```

**移动止损双机制**:
1. **EMA21 跟踪止损**: 跌破 EMA21 触发
2. **回撤止损保护**: 从最高点回撤≥2.5×ATR 触发

#### 3.2.7 订单生成模块 (core/order_generator.py)

**职责**: 生成币安 API 订单参数

**订单类型**:
```python
def generate_all_orders(
    template: OrderTemplate,
    position_qty: Decimal
) -> Dict[str, Any]:
    """
    返回:
    {
        'entry': {          # 开仓单
            'symbol': 'BTCUSDT',
            'side': 'BUY',
            'type': 'MARKET',
            'quantity': '0.001'
        },
        'stop_loss': {      # 止损单
            'symbol': 'BTCUSDT',
            'side': 'SELL',
            'type': 'STOP_MARKET',
            'stopPrice': '93000',
            'quantity': '0.001'
        },
        'take_profits': [   # 止盈单列表
            {
                'symbol': 'BTCUSDT',
                'side': 'SELL',
                'type': 'TAKE_PROFIT_MARKET',
                'stopPrice': '98200',  # TP1
                'quantity': '0.00025'  # 25%
            },
            ...
        ]
    }
    """
```

#### 3.2.8 应急处理模块 (core/emergency_handler.py)

**职责**: 极端行情检查和交易限制

**检查项**:
```python
def check_extreme_market(
    symbol: str,
    price_change_percent: Decimal,
    volatility: Decimal,
    funding_rate: Decimal
) -> bool:
    """
    一票否决项:
    - 资金费率 > 0.08%
    - 波动率 > 阈值 (BTC:4%, ETH:4.5%, BNB:7%)
    - 24h 涨幅 > 25% 或 跌幅 > 20%
    - 数据完整性不足
    """
```

**风控机制**:
```python
def is_trading_allowed() -> Tuple[bool, str]:
    """
    检查全局交易限制:
    - 日亏损限额 (5%)
    - 连续亏损限额 (5 笔)
    - 总回撤限额 (15%)
    """
```

#### 3.2.9 频率控制模块 (services/frequency_controller.py) - v6.12 新增

**职责**: 交易频率限制和冷却期管理

**限制规则**:
```python
class FrequencyController:
    """
    频率控制规则:
    1. 每日最大总交易数：4 笔 (所有等级合计)
    2. 单品种每日最大交易数：2 笔
    3. 同品种冷却期：12 小时
    4. 连续亏损暂停：5 笔亏损暂停 1 天
    5. 每日最大亏损限额：25U (500U × 5%)
    """
    
    def check_trading_limit(self, symbol: str) -> Tuple[bool, str]:
        """检查是否允许交易"""
    
    def record_trade(self, symbol: str, pnl: Decimal):
        """记录交易用于统计"""
```

**数据库表**:
```sql
CREATE TABLE trade_frequency (
    id INTEGER PRIMARY KEY,
    symbol VARCHAR(20),
    trade_time DATETIME,
    signal_grade VARCHAR(5),
    pnl DECIMAL(10,2),
    created_at DATETIME
);
```

---

## 3.3 服务层设计（v1.0.0 新增）

### 3.3.1 服务基类 (services/base.py)

**设计理念**:
- 提供统一的服务层接口
- 规范服务初始化、错误处理、日志记录等通用功能
- 支持上下文管理器和状态管理

**核心特性**:
```python
class BaseService(ABC, Generic[T]):
    """
    服务基类
    
    功能：
    1. 统一的初始化流程
    2. 统一的错误处理
    3. 统一的日志记录
    4. 统一的配置访问
    5. 上下文管理器支持
    6. 状态管理
    """
    
    # 状态管理
    - initialize()           # 初始化服务
    - start()               # 启动服务
    - stop()                # 停止服务
    - get_service_info()    # 获取服务信息
    
    # 错误处理
    - handle_error(error, context, notify)  # 处理错误
    - error_context(operation, notify, reraise)  # 错误上下文管理器
    
    # 配置访问
    - get_config_value(key_path, default, required)  # 获取配置值
    
    # 日志记录
    - log_debug(message)
    - log_info(message)
    - log_warning(message)
    - log_error(message)
    - log_critical(message)
```

**服务状态**:
```python
class ServiceState:
    UNINITIALIZED = "uninitialized"  # 未初始化
    INITIALIZING = "initializing"    # 初始化中
    READY = "ready"                  # 就绪
    RUNNING = "running"              # 运行中
    STOPPED = "stopped"              # 已停止
    ERROR = "error"                  # 错误状态
```

**使用示例**:
```python
class MyService(BaseService):
    def _initialize(self):
        # 初始化逻辑
        pass
    
    def do_something(self):
        with self.error_context("执行操作"):
            # 业务逻辑
            pass

# 使用服务
with MyService() as service:
    service.do_something()
```

### 3.3.2 数据仓库模式 (models/repository.py)

**设计理念**:
- 分离业务逻辑和数据访问逻辑
- 提供统一的数据访问接口
- 便于单元测试和Mock
- 支持查询优化和缓存

**核心特性**:
```python
class BaseRepository(ABC, Generic[T]):
    """
    数据仓库基类
    
    功能：
    1. 通用的CRUD操作
    2. 统一的错误处理
    3. 统一的日志记录
    4. 查询优化支持
    """
    
    # 基础查询方法
    - find_one(query, params) -> Optional[Dict]
    - find_many(query, params, limit) -> List[Dict]
    - find_by_id(id_value) -> Optional[Dict]
    - find_all(limit) -> List[Dict]
    
    # 插入和更新方法
    - insert(data) -> int
    - insert_and_return_id(data) -> Any
    - update(id_value, data) -> int
    - upsert(data, conflict_columns, update_columns) -> int
    - delete(id_value) -> int
    
    # 批量操作方法
    - batch_insert(data_list) -> int
    
    # 统计和查询方法
    - count(where_clause, params) -> int
    - exists(where_clause, params) -> bool
    - sum(column, where_clause, params) -> Decimal
```

**使用示例**:
```python
class TradeRepository(BaseRepository[Dict]):
    def __init__(self):
        super().__init__(table_name='trades', primary_key='id')
    
    def get_by_order_id(self, order_id: int) -> Optional[Dict]:
        return self.find_one(
            "SELECT * FROM trades WHERE order_id = %s",
            (order_id,)
        )
    
    def get_recent_trades(self, symbol: str, limit: int = 10) -> List[Dict]:
        return self.find_many(
            "SELECT * FROM trades WHERE symbol = %s ORDER BY created_at DESC",
            (symbol,),
            limit=limit
        )
```

### 3.3.3 重构的服务模块

**已重构服务** (基于BaseService):
1. **services/frequency_controller.py** - 频率控制器
   - 继承BaseService
   - 统一的错误处理和日志记录
   - 状态管理

2. **services/rule_executor.py** - 规则执行器
   - 继承BaseService
   - 统一的初始化流程
   - 错误上下文管理

3. **services/trade_executor.py** - 交易执行器
   - 继承BaseService
   - 统一的配置访问
   - 日志记录

**优势**:
- ✅ 统一的错误处理机制
- ✅ 统一的日志记录格式
- ✅ 统一的配置访问方式
- ✅ 状态管理和服务监控
- ✅ 便于单元测试和Mock

---

## 4. 数据流架构

### 4.1 实时交易数据流

```
通用 K 线服务 (http://43.156.242.184:8765)
    │
    ├─→ GET /api/v1/klines/latest    # K 线数据
    ├─→ GET /api/v1/indicators        # 技术指标
    └─→ GET /api/v1/symbols           # 支持的币种列表
    │
    ▼
utils/kline_service.py (K 线服务客户端)
    │
    ▼
core/data/fetcher.py (多时间框架整合)
    │
    ▼
core/signal/detector.py (信号检测)
    │
    ├─→ core/scoring/ (评分引擎)      # 评分
    ├─→ core/data/indicators.py       # 指标计算
    └─→ config/config_manager.py      # 参数配置
    │
    ▼
services/frequency_controller.py (频率检查)
    │
    ▼
services/position_adjuster.py (仓位调整)
    │
    ▼
core/order_generator.py (订单生成)
    │
    ▼
utils/binance_trade_api.py (API 调用)
    │
    ├─→ POST /fapi/v1/order          # 开仓
    ├─→ POST /fapi/v1/order          # 止损单
    └─→ POST /fapi/v1/order          # 止盈单
    │
    ▼
models/database.py (持久化)
    │
    ├─→ trades 表                    # 交易记录
    ├─→ trade_frequency 表           # 频率统计
    └─→ performance 表               # 绩效统计
    │
    ▼
utils/lark_notifier.py (飞书通知)
    │
    ▼
通用通知服务 (http://43.156.242.184:8766)
    │
    └─→ POST /api/v1/send            # 发送飞书消息
```

### 4.2 回测数据流

```
币安 API
    │
    ▼
scripts/fetch_multi_timeframe_data.py (数据获取脚本)
    │
    ▼
data/multi_timeframe_data.json (本地存储)
    │
    ▼
scripts/backtest_v6131_real.py (回测器)
    │
    ├─→ backtesting/technical_indicators.py  # 指标计算
    └─→ core/scoring_engine_v8.py   # 评分系统
    │
    ▼
data/backtest_v6131_real_YYYYMMDD_HHMMSS.json (回测报告)
    │
    ▼
reporting/performance_reporter.py (绩效分析)
    │
    ▼
docs/reports/v613 回测报告.md (分析报告)
```

---

## 5. 数据库设计

### 5.1 数据库表结构

```sql
-- 交易记录表
CREATE TABLE trades (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    symbol VARCHAR(20) NOT NULL,
    direction VARCHAR(10),  -- LONG/SHORT
    entry_price DECIMAL(20,8),
    stop_loss_price DECIMAL(20,8),
    take_profit_levels TEXT,  -- JSON 数组
    quantity DECIMAL(20,8),
    margin DECIMAL(20,8),
    leverage INTEGER,
    signal_grade VARCHAR(5),  -- S/A/B/C
    score DECIMAL(5,2),  -- 评分
    status VARCHAR(20),  -- OPEN/CLOSED
    pnl DECIMAL(20,8),
    pnl_percent DECIMAL(10,4),
    open_time DATETIME,
    close_time DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 频率控制表
CREATE TABLE trade_frequency (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    symbol VARCHAR(20) NOT NULL,
    trade_time DATETIME NOT NULL,
    signal_grade VARCHAR(5),
    pnl DECIMAL(10,2),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 绩效统计表
CREATE TABLE performance (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    date DATE NOT NULL,
    symbol VARCHAR(20),
    total_trades INTEGER,
    win_trades INTEGER,
    total_pnl DECIMAL(20,8),
    win_rate DECIMAL(5,4),
    max_drawdown DECIMAL(10,4),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 索引
CREATE INDEX idx_trades_symbol ON trades(symbol);
CREATE INDEX idx_trades_status ON trades(status);
CREATE INDEX idx_frequency_symbol_time ON trade_frequency(symbol, trade_time);
CREATE INDEX idx_performance_date ON performance(date);
```

### 5.2 数据库管理器

```python
# models/database.py
class DatabaseManager:
    """
    数据库管理器:
    - 初始化数据库连接
    - 创建表结构
    - 提供 CRUD 操作
    """
    
    def __init__(self, db_url: str = 'sqlite:///trading.db'):
        self.engine = create_engine(db_url)
        self.Session = sessionmaker(bind=self.engine)
    
    def create_tables(self):
        """创建所有表"""
    
    def add_trade(self, trade_data: Dict):
        """添加交易记录"""
    
    def get_trade_history(self, symbol: str = None, limit: int = 100):
        """获取交易历史"""
    
    def get_daily_performance(self, date: datetime):
        """获取每日绩效"""
```

---

## 6. 配置系统

### 6.1 配置文件层次

```
配置层次:
├─ .env                      # 环境变量 (API 密钥、Webhook 等敏感信息)
├─ config/config.yaml        # 统一配置文件 (v1.0.0 新增)
├─ config/config_manager.py  # 统一配置管理器 (v1.0.0 新增)
├─ config/strategy_params.py # 策略参数 (旧配置，待移除)
├─ config/scoring_params.yaml # 评分系统参数 (旧配置，待移除)
├─ config/v613_params.yaml   # v6.13 动态仓位参数 (旧配置，待移除)
└─ config/scoring_params_v*.yaml # 历史版本参数 (旧配置，待移除)
```

**v1.0.0 重构说明** (2026-04-27):
- ✅ 创建统一配置文件 `config/config.yaml`，整合所有非敏感配置
- ✅ 创建统一配置管理器 `config/config_manager.py`
- ✅ 支持环境变量覆盖（优先级最高）
- ✅ 提供向后兼容接口
- ⏳ 旧配置文件待移除（阶段三）

### 6.2 统一配置管理器

#### 6.2.1 配置管理器特性

**文件**: [config/config_manager.py](file:///Users/yl/vscode/bianace_btcethbnb_trade/config/config_manager.py)

**核心功能**:
- ✅ 从 YAML 文件加载配置
- ✅ 环境变量覆盖（优先级最高）
- ✅ 配置验证
- ✅ 配置缓存和热加载
- ✅ 类型转换（Decimal、int、float、bool、list）
- ✅ 向后兼容接口
- ✅ 线程安全的单例模式

**使用示例**:
```python
from config.config_manager import get_config, get_config_decimal, get_params

# 获取配置值
total_capital = get_config('account.total_capital')  # 500
risk_amount = get_config_decimal('position_sizing.risk_amount')  # Decimal('10')

# 向后兼容接口
params = get_params()  # 返回与旧接口兼容的字典
```

#### 6.2.2 环境变量 (.env)

```bash
# 币安 API 密钥
BINANCE_API_KEY=your_api_key
BINANCE_SECRET_KEY=your_secret_key

# 通用服务配置
KLINE_SERVICE_URL=http://43.156.242.184:8765/api/v1
NOTIFICATION_SERVICE_URL=http://43.156.242.184:8766/api/v1
NOTIFICATION_PROJECT=btc_eth_bnb

# 飞书 Webhook（已废弃，使用通用通知服务）
# LARK_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/xxx

# 数据库连接
DATABASE_URL=sqlite:///trading.db

# 环境配置
ENVIRONMENT=production  # production/development
BINANCE_TESTNET=false   # true/false
TIMEZONE=Asia/Shanghai
```

#### 6.2.3 统一配置文件 (config/config.yaml)

**文件**: [config/config.yaml](file:///Users/yl/vscode/bianace_btcethbnb_trade/config/config.yaml)

**配置结构**:
```yaml
# 系统配置
system:
  name: "Binance Trading System"
  version: "6.13.2"
  environment: "production"
  timezone: "Asia/Shanghai"

# 账户配置
account:
  total_capital: 500  # 总资金（U）
  single_position_margin: 15  # 单仓保证金（U）
  max_positions: 2  # 最大同时持仓数
  reserve_capital_ratio: 0.8  # 备用金比例（80%）
  max_total_margin_ratio: 0.3  # 最大总保证金占用比例（30%）

# 信号等级定义
signal_grades:
  S:
    min_profit_loss_ratio: 4.0  # 盈亏比 ≥ 1:4
    max_leverage: 5  # 杠杆上限 5 倍
    min_recommendation_score: 85  # 推荐度 ≥ 85 分
    require_multi_timeframe: true  # 需要多时间框架共振
  A:
    min_profit_loss_ratio: 3.0
    max_leverage: 4
    min_recommendation_score: 70
    require_multi_timeframe: true
  B:
    min_profit_loss_ratio: 2.5
    max_leverage: 3
    min_recommendation_score: 50
    require_multi_timeframe: false

# 仓位管理
position_sizing:
  risk_amount: 10  # 单笔风险金额（总资金的 2% = 10U）
  min_stop_loss_pct: 0.02  # 最小止损幅度 2%
  max_stop_loss_pct: 0.04  # 最大止损幅度 4%
  max_position_notional: 1500  # 单品种最大名义价值
  max_total_notional: 4000  # 所有持仓最大名义价值
  
  # 仓位系数机制
  position_coefficient:
    S: 0.5  # S 级：50%
    A: 0.3  # A 级：30%
    B: 0.2  # B 级：20%

# 风险管理
risk_management:
  stop_loss_multiplier: 1.0  # 止损倍数
  take_profit_levels:
    tp1_ratio: 0.3  # TP1 平仓 30%
    tp1_multiplier: 4.0  # TP1 = 4.0×ATR
    tp2_ratio: 0.3  # TP2 平仓 30%
    tp2_multiplier: 6.0  # TP2 = 6.0×ATR
    tp3_ratio: 0.4  # TP3 平仓 40%
  
  margin_ratio_warning: 1.5  # 保证金率预警线
  margin_ratio_emergency: 1.2  # 保证金率紧急线
  max_margin_usage: 0.6  # 保证金使用率上限
  max_float_loss: 20  # 单笔浮亏上限

# 评分系统配置
scoring:
  # 评分维度权重
  weights:
    trend_strength: 0.40   # 趋势强度 40%
    pattern_quality: 0.35  # 形态质量 35%
    momentum_divergence: 0.25  # 动量背离 25%
  
  # 信号等级阈值
  grade_thresholds:
    S: 85
    A: 75
    B: 65
    C: 55
  
  # 币种差异化配置
  symbols:
    BTCUSDT:
      volatility_threshold: 0.04
      breakout_threshold: 0.015
    ETHUSDT:
      volatility_threshold: 0.045
      breakout_threshold: 0.015
    BNBUSDT:
      volatility_threshold: 0.07
      breakout_threshold: 0.025
```

#### 6.2.4 向后兼容性

为了保持与现有代码的兼容性，提供了以下接口：

```python
# 旧接口（config/strategy_params.py）
from config.strategy_params import get_params, get_param, set_param

# 新接口（config/config_manager.py）
from config.config_manager import get_params, get_param, set_param

# 两者功能完全一致，无需修改现有代码
```

**迁移策略**:
1. **阶段一**（当前）：新旧接口并存，优先使用新接口
2. **阶段二**：逐步更新现有模块使用新接口
3. **阶段三**：移除旧配置文件

---

## 7. 部署架构

### 7.1 Docker 容器化部署

#### 7.1.1 Dockerfile

```dockerfile
FROM python:3.11-slim

WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    gcc \
    postgresql-client \
    && rm -rf /var/lib/apt/lists/*

# 安装 Python 依赖
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制应用代码
COPY . .

# 创建日志目录
RUN mkdir -p /app/logs

# 启动命令
CMD ["python", "scheduler_new.py"]
```

#### 7.1.2 docker-compose.yml

```yaml
version: '3.8'

services:
  binance-trade-analyzer:
    build: .
    container_name: binance-trade-analyzer
    restart: unless-stopped
    environment:
      - BINANCE_API_KEY=${BINANCE_API_KEY}
      - BINANCE_SECRET_KEY=${BINANCE_SECRET_KEY}
      - LARK_WEBHOOK_URL=${LARK_WEBHOOK_URL}
      - DATABASE_URL=sqlite:///app/trading.db
      - ENVIRONMENT=production
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./trading.db:/app/trading.db
    networks:
      - trading_network

networks:
  trading_network:
    driver: bridge
```

### 7.2 部署脚本

#### 7.2.1 一键部署脚本 (deploy_v613.sh)

```bash
#!/bin/bash

# v6.13 部署脚本

echo "🚀 开始部署 v6.13..."

# 1. 打包核心文件
tar -czf deployment_package.tar.gz \
    core/scoring_engine_v8.py \
    core/position_calculator.py \
    core/risk_manager.py \
    core/order_generator.py \
    core/signal_detector.py \
    core/data_fetcher.py \
    core/emergency_handler.py \
    services/frequency_controller.py \
    services/position_adjuster.py \
    config/strategy_params.py \
    config/scoring_params.yaml \
    config/v613_params.yaml \
    scheduler_new.py \
    utils/ \
    models/ \
    requirements.txt

# 2. 上传到服务器
scp deployment_package.tar.gz root@43.156.242.184:/root/

# 3. SSH 登录并部署
ssh root@43.156.242.184 << 'EOF'
cd /root
tar -xzf deployment_package.tar.gz -C /root/binance-trade-analyzer/
docker restart binance-trade-analyzer
echo "✅ v6.13 部署完成!"
docker logs -f binance-trade-analyzer
EOF

echo "✅ 部署完成!"
```

### 7.3 服务器目录结构

```
/root/
└── binance-trade-analyzer/
    ├── core/                      # 核心模块
    │   ├── scoring_engine_v8.py   # v6.12 评分引擎
    │   ├── position_calculator.py # 仓位计算
    │   ├── risk_manager.py        # 风险管理
    │   ├── order_generator.py     # 订单生成
    │   ├── signal_detector.py     # 信号检测
    │   ├── data_fetcher.py        # 数据获取
    │   └── emergency_handler.py   # 应急处理
    ├── services/                  # 服务模块
    │   ├── frequency_controller.py # v6.12 频率控制
    │   └── position_adjuster.py   # v6.13 仓位调整
    ├── config/                    # 配置文件
    │   ├── strategy_params.py
    │   ├── scoring_params.yaml
    │   └── v613_params.yaml
    ├── utils/                     # 工具模块
    ├── models/                    # 数据模型
    ├── scheduler_new.py           # 调度器
    ├── requirements.txt           # 依赖
    ├── trading.db                 # SQLite 数据库
    ├── logs/                      # 日志目录
    └── data/                      # 数据目录
```

---

## 8. 日志系统与错误处理

### 8.1 统一日志系统 (v1.0.0 新增)

#### 8.1.1 日志配置器

**文件**: [utils/logger.py](file:///Users/yl/vscode/bianace_btcethbnb_trade/utils/logger.py)

**核心功能**:
- ✅ 统一的日志格式
- ✅ 日志级别定义（DEBUG/INFO/WARNING/ERROR/CRITICAL）
- ✅ 日志文件管理（RotatingFileHandler）
- ✅ 多处理器支持（控制台 + 文件）
- ✅ 错误日志单独记录

**日志文件**:
- `logs/trading_system.log` - 主日志文件（所有级别）
- `logs/error.log` - 错误日志文件（ERROR 及以上）

**使用示例**:
```python
from utils.logger import init_logging, get_logger

# 初始化日志系统
init_logging()

# 获取日志记录器
logger = get_logger('my_module')

# 记录日志
logger.info("这是一条信息日志")
logger.error("这是一条错误日志")
```

#### 8.1.2 日志级别和场景

```python
# DEBUG: 详细调试信息
logger.debug(f"计算 EMA21 斜率：{slope}")

# INFO: 正常运营信息
logger.info(f"检测到 {symbol} {grade}级信号，评分：{score}")

# WARNING: 警告信息
logger.warning(f"{symbol} 资金费率 {funding_rate:.4%} 接近阈值")

# ERROR: 错误信息
logger.error(f"订单执行失败：{error}", exc_info=True)

# CRITICAL: 严重错误
logger.critical(f"连续亏损 5 笔，暂停交易 1 天")
```

#### 8.1.3 日志文件管理

```
logs/
├── trading_system.log          # 主日志文件（所有级别）
├── error.log                   # 错误日志文件（ERROR 及以上）
├── scheduler_new.log           # 调度器日志（旧）
├── backtest_v613_multi.log     # 回测日志（旧）
└── 2026-04-12.log              # 按日期分割的日志 (RotatingFileHandler)
```

### 8.2 异常处理系统 (v1.0.0 新增)

#### 8.2.1 自定义异常类

**文件**: [utils/exceptions.py](file:///Users/yl/vscode/bianace_btcethbnb_trade/utils/exceptions.py)

**异常层次结构**:
```
TradingSystemError (基础异常)
├── ConfigurationError (配置错误)
│   ├── ConfigFileNotFoundError
│   └── ConfigValidationError
├── DataError (数据错误)
│   ├── DataFetchError
│   ├── DataValidationError
│   └── InsufficientDataError
├── TradingError (交易错误)
│   ├── OrderExecutionError
│   ├── InsufficientBalanceError
│   ├── RiskLimitExceededError
│   └── SignalNotFoundError
├── APIError (API 错误)
│   ├── APIConnectionError
│   ├── APIResponseError
│   ├── APIRateLimitError
│   └── APIAuthenticationError
├── NotificationError (通知错误)
│   └── LarkNotificationError
└── DatabaseError (数据库错误)
    ├── DatabaseConnectionError
    └── DatabaseQueryError
```

**使用示例**:
```python
from utils.exceptions import InsufficientBalanceError

# 抛出异常
raise InsufficientBalanceError(
    required=100.0,
    available=50.0,
    currency="USDT"
)

# 异常信息
# [TRADING_ERROR_USDT] 余额不足：需要 100.0 USDT，可用 50.0 USDT
```

#### 8.2.2 统一错误处理器

**文件**: [utils/error_handler.py](file:///Users/yl/vscode/bianace_btcethbnb_trade/utils/error_handler.py)

**核心功能**:
- ✅ 统一的错误日志格式
- ✅ 错误分类和级别
- ✅ 错误统计和历史记录
- ✅ 错误通知机制（集成飞书）
- ✅ 错误处理装饰器

**使用示例**:
```python
from utils.error_handler import handle_error, error_handler

# 方式 1: 手动处理
try:
    # 可能抛出异常的代码
    pass
except Exception as e:
    error_info = handle_error(e, context={'module': 'test'}, notify=True)

# 方式 2: 装饰器
@error_handler(notify_on_error=True, reraise=False, default_return=None)
def some_function():
    # 可能抛出异常的代码
    pass
```

### 8.3 旧日志配置（待移除）

```python
# scheduler_new.py
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[
        logging.StreamHandler(),  # 控制台输出
        logging.FileHandler(
            'logs/scheduler_new.log',
            encoding='utf-8'
        )  # 文件输出
    ]
)
```

---

## 9. 监控与告警

### 9.1 飞书通知系统

#### 9.1.1 通知场景

```python
# utils/lark_notifier.py

class LarkNotifier:
    """
    飞书通知场景（通过通用通知服务）:
    1. 检测到交易信号 (S/A/B 级)
    2. 执行交易 (开仓/平仓)
    3. 停止交易 (风控触发)
    4. 系统异常 (分析失败)
    5. 交易日报 (每日 9:00)
    
    改造说明：
    - 保持原有接口不变，确保向后兼容
    - 内部实现改为调用通用通知服务
    - 通用服务地址：http://43.156.242.184:8766/api/v1
    - 项目标识：btc_eth_bnb
    """
    
    def send_text_message(self, content, level='info'):
        """发送文本消息"""
    
    def send_success_notification(self, currency, report_path, screenshot_path):
        """发送成功通知"""
    
    def send_error_notification(self, currency, error_message):
        """发送错误通知"""
    
    def send_analysis_result_notification(self, currencies, report_content):
        """发送分析结果通知"""
    
    def send_scheduler_startup_notification(self, next_run_time, timezone):
        """发送调度器启动通知"""
    
    def send_scheduler_shutdown_notification(self, timezone):
        """发送调度器关闭通知"""
```

#### 9.1.2 通知模板

```python
# 信号通知模板
{
    "msg_type": "interactive",
    "card": {
        "header": {
            "title": "🎯 检测到交易信号",
            "tag_style": "blue"
        },
        "elements": [
            {
                "tag": "div",
                "text": "**币种**: BTCUSDT\n**方向**: 做多\n**等级**: A 级\n**评分**: 78.5\n**建议仓位**: 30%\n**杠杆**: 4 倍"
            },
            {
                "tag": "div",
                "text": "**入场价**: 95000\n**止损价**: 93800 (-1.26%)\n**止盈 1**: 98200 (+3.37%)\n**止盈 2**: 100600 (+5.89%)"
            }
        ]
    }
}
```

### 9.2 数据库监控

```sql
-- 查询今日交易统计
SELECT 
    symbol,
    COUNT(*) as total_trades,
    SUM(CASE WHEN pnl > 0 THEN 1 ELSE 0 END) as win_trades,
    SUM(pnl) as total_pnl,
    AVG(pnl) as avg_pnl
FROM trades
WHERE DATE(open_time) = DATE('now')
GROUP BY symbol;

-- 查询连续亏损次数
SELECT COUNT(*) as consecutive_losses
FROM (
    SELECT pnl
    FROM trades
    WHERE status = 'CLOSED'
    ORDER BY close_time DESC
    LIMIT 10
)
WHERE pnl < 0;
```

---

## 10. 回测系统

### 10.1 回测器架构

```python
# backtesting/multi_timeframe_backtester_v55_full.py

class MultiTimeframeBacktester:
    """
    多时间框架回测器:
    1. 加载历史数据 (JSON)
    2. 模拟时间序列遍历
    3. 调用真实评分引擎
    4. 计算仓位和止损止盈
    5. 模拟成交和盈亏
    6. 统计绩效指标
    """
    
    def __init__(
        self,
        data: Dict,
        initial_capital: Decimal = Decimal('500'),
        start_date: datetime = None,
        end_date: datetime = None
    ):
        pass
    
    def run(self) -> BacktestReport:
        """运行回测"""
    
    def _simulate_trading_day(self, date: datetime):
        """模拟单日交易"""
    
    def _calculate_metrics(self) -> Dict:
        """计算绩效指标"""
```

### 10.2 回测指标

```python
回测报告指标:
├─ 总交易数 (Total Trades)
├─ 胜率 (Win Rate)
├─ 盈亏比 (Profit/Loss Ratio)
├─ 总收益率 (Total Return)
├─ 年化收益率 (Annualized Return)
├─ 最大回撤 (Max Drawdown)
├─ 夏普比率 (Sharpe Ratio)
├─ 平均持仓时间 (Avg Holding Period)
└─ 按信号等级统计 (Grade Statistics)
    ├─ S 级交易数/胜率/盈亏
    ├─ A 级交易数/胜率/盈亏
    ├─ B 级交易数/胜率/盈亏
    └─ C 级交易数/胜率/盈亏
```

### 10.3 回测数据流

```bash
# 1. 获取历史数据
python3 scripts/fetch_multi_timeframe_data.py \
  --symbols BTCUSDT,ETHUSDT,BNBUSDT \
  --days 180 \
  --output data/multi_timeframe_data.json

# 2. 运行回测
python3 scripts/backtest_v6131_real.py \
  --data data/multi_timeframe_data.json \
  --capital 500 \
  --output data/backtest_v6131_real.json

# 3. 查看报告
cat data/backtest_v6131_real.json | jq '.summary'
```

---

## 11. 版本演进

### 11.1 版本历史

| 版本 | 日期 | 核心改进 | 文件变更 |
|------|------|---------|---------|
| **v6.13.1** | 2026-04-12 | 优化止盈止损 + 时间止损 | scheduler_new.py, backtest_v6131*.py |
| **v6.13** | 2026-04-10 | 动态仓位调整器 | position_adjuster.py, v613_params.yaml |
| **v6.12** | 2026-04-07 | 量化评分系统 + 频率控制 | scoring_engine_v8.py, frequency_controller.py |
| **v5.5** | 2026-04-07 | 评分系统 (初版) | scoring_engine.py |
| **v5.3** | 2026-04-02 | 仓位系数 + ATR 动态止损 | position_calculator.py, risk_manager.py |
| **v5.2** | 2026-03-25 | 多时间框架回测 | backtester_v55_full.py |
| **v1.0.0** | 2026-04-01 | 核心交易模块 | 所有核心模块 |

### 11.2 关键改进对比

#### v6.12 vs v5.3

| 维度 | v5.3 | v6.12 | 改善 |
|------|------|-------|------|
| **信号分级** | 主观判断 | 量化评分 (100 分制) | 客观化 |
| **仓位管理** | 固定比例 | 评分线性映射 | 精细化 |
| **频率控制** | 无 | 每日 4 笔 + 冷却期 | 降低过度交易 |
| **币种差异化** | 无 | BTC/ETH/BNB 不同阈值 | 适配波动率 |

#### v6.13 vs v6.12

| 场景 | v6.12 | v6.13 | 改善 |
|------|-------|-------|------|
| **资金充足 (100U+)** | 执行 | 执行 | = 相同 |
| **资金略不足 (50-100U)** | ❌ 跳过 | ✅ 执行 (60-90%) | ↑ 成功率 |
| **资金紧张 (20-50U)** | ❌ 跳过 | ✅ 执行 (30-60%) | ↑ 机会 |

#### v6.13.1 vs v6.13

| 指标 | v6.13 | v6.13.1 | 改善 |
|------|-------|---------|------|
| **胜率** | 79.8% | 100% | ↑ +20.2% |
| **最大回撤** | 0.9% | 0.0% | ↓ -100% |
| **持仓时间** | 90.9 小时 | 59.9 小时 | ↓ -34% |
| **夏普比率** | 9.32 | 22.56 | ↑ +142% |
| **总盈利** | +698U | +639U | ↓ -8.5% |

---

## 12. 最佳实践

### 12.1 开发规范

1. **模块命名**: 使用 `snake_case` 命名法
2. **类型注解**: 所有函数添加类型注解
3. **文档字符串**: 所有类和函数添加 docstring
4. **日志记录**: 关键操作记录日志
5. **异常处理**: 使用 try-except 包裹外部调用
6. **配置分离**: 硬编码参数放入配置文件

### 12.2 测试规范

```python
# tests/test_scoring_engine.py

import unittest
from core.scoring_engine_v8 import ScoringEngine

class TestScoringEngine(unittest.TestCase):
    
    def setUp(self):
        self.engine = ScoringEngine()
    
    def test_score_calculation(self):
        """测试评分计算"""
        data = {...}  # 测试数据
        result = self.engine.score('BTCUSDT', data)
        self.assertIn(result['grade'], ['S', 'A', 'B', 'C'])
    
    def test_veto_conditions(self):
        """测试一票否决"""
        data = {...}  # 高资金费率数据
        result = self.engine.score('BTCUSDT', data)
        self.assertIsNone(result)  # 应被否决
```

### 12.3 部署检查清单

```markdown
部署前检查:
- [ ] 配置文件已更新 (strategy_params.py, scoring_params.yaml)
- [ ] .env 文件已设置 (API 密钥、Webhook)
- [ ] 数据库已备份 (trading.db)
- [ ] 依赖已安装 (pip install -r requirements.txt)
- [ ] 测试已通过 (python -m pytest tests/)

部署后验证:
- [ ] 容器已启动 (docker ps)
- [ ] 日志正常 (docker logs -f binance-trade-analyzer)
- [ ] 数据库连接正常
- [ ] 飞书通知正常
- [ ] 定时任务已启动
```

---

## 13. 故障排查

### 13.1 常见问题

#### Q1: 容器启动失败

```bash
# 查看容器日志
docker logs binance-trade-analyzer

# 检查配置文件
docker exec binance-trade-analyzer cat /app/.env

# 重启容器
docker restart binance-trade-analyzer
```

#### Q2: 交易信号过多

**原因**: 评分阈值过低或频率控制失效

**解决方案**:
```python
# 1. 提高评分阈值
config/strategy_params.py:
    'grade_thresholds': {
        'S': 90,  # 从 85 提高到 90
        'A': 80,  # 从 75 提高到 80
    }

# 2. 检查频率控制器
services/frequency_controller.py:
    DAILY_MAX_TRADES = 4  # 确保设置为 4
```

#### Q3: 止损过于频繁

**原因**: ATR 倍数过低或波动率阈值过高

**解决方案**:
```python
# 1. 调整止损倍数
config/strategy_params.py:
    'stop_loss_atr_multiplier': 2.0  # 从 1.5 提高到 2.0

# 2. 调整波动率阈值
config/scoring_params.yaml:
    symbols:
      BTCUSDT:
        volatility_threshold: 0.03  # 从 4% 降到 3%
```

### 13.2 日志分析

```bash
# 查看错误日志
grep "ERROR" logs/scheduler_new.log

# 查看信号检测日志
grep "检测到" logs/scheduler_new.log | tail -20

# 查看交易执行日志
grep "执行交易" logs/scheduler_new.log | tail -10

# 统计每日交易数
grep "执行交易" logs/scheduler_new.log | \
  awk '{print $1}' | sort | uniq -c
```

---

## 14. 性能优化

### 14.1 评分性能

```python
# 优化前：每次重新计算指标
def score(self, symbol, data):
    indicators = self._calculate_all_indicators(data)  # 耗时 200ms
    score = self._calculate_score(indicators)  # 耗时 50ms
    return score

# 优化后：缓存指标计算结果
@lru_cache(maxsize=100)
def _calculate_indicators(self, data_hash):
    # 缓存指标计算结果
    pass

def score(self, symbol, data):
    data_hash = hash(str(data))
    indicators = self._calculate_indicators(data_hash)  # 耗时 10ms (缓存命中)
    score = self._calculate_score(indicators)  # 耗时 50ms
    return score  # 总耗时从 250ms 降至 60ms
```

### 14.2 数据库性能

```sql
-- 添加索引优化查询性能
CREATE INDEX idx_trades_symbol_time ON trades(symbol, open_time);
CREATE INDEX idx_frequency_symbol_status ON trade_frequency(symbol, trade_time);

-- 使用连接池
from sqlalchemy import create_engine
engine = create_engine(
    'sqlite:///trading.db',
    pool_size=10,
    max_overflow=20,
    pool_recycle=3600
)
```

### 14.3 API 调用优化

```python
# 使用会话复用
import requests
session = requests.Session()
session.mount('https://', HTTPAdapter(pool_connections=10, pool_maxsize=10))

# 批量获取 K 线数据
def fetch_multi_timeframe_data(symbols, timeframes):
    # 并行请求 (使用 asyncio)
    # 而不是串行请求
    pass
```

---

## 15. 安全与合规

### 15.1 API 密钥安全

```bash
# 1. 使用环境变量存储密钥
echo "BINANCE_API_KEY=xxx" >> .env
echo "BINANCE_SECRET_KEY=xxx" >> .env

# 2. .env 文件不提交到 Git
echo ".env" >> .gitignore

# 3. 设置文件权限
chmod 600 .env

# 4. 使用币安子账户密钥 (限制权限)
# 只授予交易权限，不授予提现权限
```

### 15.2 数据备份

```bash
#!/bin/bash
# backup.sh - 每日备份脚本

BACKUP_DIR="/backup/trading_$(date +%Y%m%d)"
mkdir -p $BACKUP_DIR

# 备份数据库
cp trading.db $BACKUP_DIR/

# 备份配置文件
cp config/*.py config/*.yaml $BACKUP_DIR/

# 备份日志
cp -r logs/ $BACKUP_DIR/

# 压缩备份
tar -czf $BACKUP_DIR.tar.gz $BACKUP_DIR

# 删除 30 天前的备份
find /backup -name "trading_*.tar.gz" -mtime +30 -delete
```

---

## 16. 未来规划

### 16.1 功能扩展

1. **多策略支持**: 支持并行运行多个策略
2. **实时风控**: 持仓期间实时监控和动态调整
3. **机器学习**: 使用 ML 模型优化评分系统
4. **Web 控制台**: 可视化的交易监控和管理界面
5. **回测优化**: 支持参数网格搜索和优化

### 16.2 技术升级

1. **数据库迁移**: 从 SQLite 迁移到 PostgreSQL
2. **异步化**: 使用 asyncio 提升并发性能
3. **微服务化**: 将核心模块拆分为独立服务
4. **消息队列**: 使用 Redis/RabbitMQ 进行模块间通信
5. **监控告警**: 集成 Prometheus + Grafana

---

## 附录

### A. 术语表

| 术语 | 说明 |
|------|------|
| PM 账户 | 投资组合保证金账户 (Portfolio Margin) |
| ATR | 平均真实波幅 (Average True Range) |
| ADX | 平均趋向指标 (Average Directional Index) |
| EMA | 指数移动平均线 (Exponential Moving Average) |
| MACD | 移动平均收敛发散 (Moving Average Convergence Divergence) |
| RSI | 相对强弱指标 (Relative Strength Index) |
| 吊灯止损 | Trailing Stop Loss (从最高点回撤触发) |

### B. 参考文档

- [币安 API 文档](https://binance-docs.github.io/apidocs/futures/cn/)
- [traderule.txt](config/traderule.txt) - 交易规则原文
- [docs/reports/](docs/reports/) - 各版本报告
- [docs/proposals/](docs/proposals/) - 技术方案文档

### C. 文件清单

```
核心文件:
├── scheduler_new.py                    # 主调度器
├── core/scoring_engine_v8.py           # v6.12 评分引擎
├── core/position_calculator.py         # 仓位计算器
├── core/risk_manager.py                # 风险管理器
├── core/order_generator.py             # 订单生成器
├── core/signal_detector.py             # 信号检测器
├── core/data_fetcher.py                # 数据获取器
├── core/emergency_handler.py           # 应急处理器
├── services/frequency_controller.py    # v6.12 频率控制器
├── services/position_adjuster.py       # v6.13 仓位调整器
├── config/strategy_params.py           # 策略参数
├── config/scoring_params.yaml          # 评分参数
├── config/v613_params.yaml             # v6.13 参数
└── utils/biance_trade_api.py           # 币安交易 API

回测文件:
├── scripts/backtest_v6131_real.py      # v6.13.1 回测器
├── scripts/backtest_v6131_full.py      # v6.13.1 完整版回测
├── scripts/fetch_multi_timeframe_data.py # 数据获取脚本
└── backtesting/multi_timeframe_backtester_v55_full.py # 回测引擎

部署文件:
├── Dockerfile                          # Docker 配置
├── docker-compose.yml                  # Docker Compose
├── deploy_v613.sh                      # v6.13 部署脚本
├── deploy_v6131.sh                     # v6.13.1 部署脚本
└── .env                                # 环境变量

文档:
├── readme.md                           # 项目主文档
├── docs/README.md                      # 文档目录索引
├── docs/design/                        # 设计文档
├── docs/proposals/                     # 方案文档
├── docs/reports/                       # 报告文档
└── docs/deployment/                    # 部署文档
```

---

**文档版本**: v1.2 (2026-04-27)  
**最后更新**: 2026-04-27  
**维护者**: 系统架构团队

**更新记录**:
- v1.2 (2026-04-27): 更新通用服务集成
  - 更新数据流架构（使用通用 K 线服务和通知服务）
  - 更新支撑服务层（新增通用服务）
  - 更新环境变量配置（新增通用服务配置）
  - 更新飞书通知系统（改造为使用通用通知服务）
- v1.1 (2026-04-27): 更新模块实现细节
  - 更新数据获取模块（增强版并发支持、性能统计）
  - 更新信号检测模块（Decimal 类型安全、评分引擎集成）
  - 更新信号过滤器（类型安全转换、参数说明）
- v1.0 (2026-04-27): 更新阶段二、阶段三重构内容
  - 更新模块依赖关系（scheduler、signal、scoring、data模块拆分）
  - 新增服务层设计（BaseService、Repository Pattern）
  - 更新核心模块职责说明
  - 添加重构改进点说明
- v0.1 (2026-04-12): 初始版本
