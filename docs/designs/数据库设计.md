# 数据库设计 V3

## 1. 设计原则

1. **兼容 V2**：klines 表新增 frequency 字段但不影响 V2 查询（设默认值）
2. **务实复用**：V3 直接沿用 V2 的 scan_results 表和 push_history 表，不新建独立的信号表
3. **最小改动**：尽可能复用现有表和索引
4. **向后兼容**：V2 代码无需修改即可正常运行

## 2. 完整表结构（5 张表）

V3 与 V2 共享同一 PostgreSQL 实例的 `schema_stockfilter` schema，共使用 5 张业务表：

| 表名 | 用途 | V3 变更 |
|------|------|---------|
| `stocks` | 股票列表 | 无变更 |
| `klines` | K 线数据 | **新增 frequency 字段，唯一约束调整** |
| `scan_results` | 扫描结果 | 沿用 V2 |
| `positions` | 持仓记录 | 沿用 V2 |
| `push_history` | 推送历史（去重） | 沿用 V2 |

## 3. klines 表（V3 唯一的结构变更）

### 3.1 V3 表结构

```sql
CREATE TABLE IF NOT EXISTS klines (
    id SERIAL PRIMARY KEY,
    code VARCHAR(20) NOT NULL,
    frequency VARCHAR(10) NOT NULL DEFAULT 'd',
    date DATE NOT NULL,
    open NUMERIC(10,2),
    high NUMERIC(10,2),
    low NUMERIC(10,2),
    close NUMERIC(10,2),
    volume BIGINT,
    amount NUMERIC(20,2),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(code, frequency, date)
);

CREATE INDEX IF NOT EXISTS idx_klines_code ON klines(code);
CREATE INDEX IF NOT EXISTS idx_klines_date ON klines(date);
CREATE INDEX IF NOT EXISTS idx_klines_frequency ON klines(frequency);
```

### 3.2 V2 -> V3 迁移

迁移逻辑在 `data/database.py` 的 `_migrate_add_frequency()` 方法中自动执行：

```python
def _migrate_add_frequency(self):
    """V3 迁移：为旧版 klines 表添加 frequency 字段"""
    # 1. 检查 frequency 字段是否存在
    # 2. 不存在则新增：ALTER TABLE klines ADD COLUMN frequency VARCHAR(10) DEFAULT 'd'
    # 3. 删除旧的 (code, date) 唯一约束
    # 4. 创建新的 (code, frequency, date) 唯一约束
```

## 4. 频率（frequency）字段说明

| 值 | 含义 | 当前使用状态 |
|----|------|------------|
| `d` | 日线 | **活跃使用** -- 所有策略默认频率 |
| `w` | 周线 | 预留，klines 表已支持 |
| `m` | 月线 | 预留，klines 表已支持 |

> **注意**：频率值使用单字符缩写（`'d'`/`'w'`/`'m'`），而非全称（`'daily'`/`'weekly'`/`'monthly'`）。这与旧版设计文档不同。

## 5. 沿用 V2 的表说明

### 5.1 scan_results（扫描结果表）

```sql
CREATE TABLE IF NOT EXISTS scan_results (
    id SERIAL PRIMARY KEY,
    scan_date DATE NOT NULL,
    code VARCHAR(20) NOT NULL,
    name VARCHAR(100),
    score NUMERIC(5,2),
    surge_date DATE,
    support_level NUMERIC(10,2),
    current_close NUMERIC(10,2),
    drop_rate NUMERIC(10,4),
    min_vol_ratio NUMERIC(10,4),
    surge_price NUMERIC(10,2),
    surge_volume_ratio NUMERIC(10,4),
    surge_pct NUMERIC(10,4),
    low_after_surge NUMERIC(10,2),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

V3 通过 `DatabaseManager.save_scan_result()` 方法写入此表。注意：`scan_results` 的字段是围绕超跌反弹策略形态特征设计的，这是 V2 的历史设计。

### 5.2 push_history（推送历史表/去重表）

```sql
CREATE TABLE IF NOT EXISTS push_history (
    id SERIAL PRIMARY KEY,
    code VARCHAR(20) NOT NULL,
    push_date DATE NOT NULL,
    push_type VARCHAR(50),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(code, push_date)
);
```

去重逻辑：`(code, push_date)` 唯一约束确保同一股票同一天不会被重复推送。

### 5.3 positions（持仓表）和 stocks（股票列表表）

沿用 V2 结构，无变更。

## 6. 关于"不存在的表"

以下表在原设计文档中提及但**实际代码未创建**：

| 原计划表名 | 当前状态 | 原因 |
|-----------|---------|------|
| `strategy_signals` | **不存在** | V3 务实路线，直接沿用 V2 的 `scan_results` 表 |
| `strategy_push_history` | **不存在** | V3 务实路线，直接沿用 V2 的 `push_history` 表 |

## 7. ER 图（实际表结构）

```
+----------------+      +---------------------+
|    stocks      |      |      klines         |
+----------------+      +---------------------+
| code (PK)      |----->| code (FK)           |
| name           |      | date                |
| symbol         |      | frequency (V3 新增) |
| list_date      |      | open/high/low       |
| sector         |      | close/volume/amount |
+----------------+      +---------------------+

+------------------+    +--------------------------+
|   scan_results   |    |     push_history         |
+------------------+    +--------------------------+
| scan_date        |    | code                     |
| code             |    | push_date                |
| name             |    | push_type                |
| score            |    | UNIQUE(code, push_date)  |
| surge_date       |    +--------------------------+
| support_level    |
| ...              |    +--------------------------+
+------------------+    |     positions            |
                        +--------------------------+
                        | code                     |
                        | entry_date / entry_price |
                        | status (open/closed)     |
                        | pnl / pnl_pct            |
                        +--------------------------+
```

## 8. 数据库连接方式

V3 通过 `data/database.py` 中的 `DatabaseManager` 连接 PostgreSQL。连接信息通过环境变量读取：

| 环境变量 | 默认值 | 说明 |
|---------|--------|------|
| `DB_HOST` | localhost | 数据库主机 |
| `DB_PORT` | 5432 | 数据库端口 |
| `DB_NAME` | stockfilter | 数据库名 |
| `DB_USER` | stockfilter_user | 数据库用户 |
| `DB_PASSWORD` | Stock@2024 | 数据库密码 |
| `DB_SCHEMA` | schema_stockfilter | PostgreSQL schema |

---

**文档版本**：V2.0（根据实际代码更新）
**最后更新**：2026-05-15
